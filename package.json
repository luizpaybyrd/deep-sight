{
  "name": "deep-sight",
  "version": "1.0.0",
  "private": true,
  "description": "Spectral lure colour simulator — see how a lure's colour reaches a fish's eye through real water.",
  "scripts": {
    "serve": "python3 -m http.server 8777 --bind 127.0.0.1 -d www",
    "sync": "cap sync",
    "ios": "cap sync ios && cap open ios",
    "android": "cap sync android && cap open android",
    "android:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
